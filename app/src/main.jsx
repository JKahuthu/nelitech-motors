import React from 'react';
import ReactDOM from 'react-dom/client';
import NelitechMotors from './App.jsx';
import './index.css'; // optional, only if you want styles

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <NelitechMotors />
  </React.StrictMode>
);

